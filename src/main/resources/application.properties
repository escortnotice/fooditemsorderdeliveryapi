# For openshift deployment create this application.properties as configmap with below command
# > oc create configmap fooditemsorderdeliveryapi-props-configmap --from-file=src/main/resources/application.properties

spring.profiles.active=@spring.profiles.active@

spring.application.name=fooditemsorderdeliveryapi-exp

application.health.message = OK

#spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

#spring.jpa.hibernate.ddl-auto=none

#For logging
logging.level.org.springframework=INFO

#output to a temp_folder/file
#logging.file.name=application_logs.log
